# SOL2CPN_Tool

phần Analyser
```
1. compile solidity file bằng solc dùng option "--ast-compact-json"
2. chạy các file run.sh trong folder ./include -> ./src -> ./ để complie c++ code (có thể dùng visual studio hoặc ide tương tự để compile nhanh hơn ko cần chạy các file run.sh)
```